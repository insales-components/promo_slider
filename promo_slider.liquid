{% if block_lists.with_template['system-promo-slide'].promo-slider.blocks.size > 0 %}
  <div class="promo_slider-wrapper">
    <div class="promo_slider swiper-container">
      <div class="swiper-wrapper">

        {% for slide in block_lists.with_template['system-promo-slide'].promo-slider.blocks %}
          <div class="promo-slide swiper-slide">
            {% assign tag = 'div' %}
            {% assign attr = '' %}
            {% if slide.link.size > 0 %}
              {% assign tag = 'a' %}
              {% capture attr %}href="{{slide.link}}"{% endcapture %}
            {% endif %}

            <{{tag}} {{ attr }} class="slide-inner">

              <img class="slide-image" src="http://fakeimg.pl/1920x700/d5d5d5/" />
              <!-- <img class="slide-image" src="{{ slide.image }}" /> -->

              <div class="slide-absolute">
                <div class="container slide-content">
                  <div class="slide-title">
                    {{ slide.title }}
                  </div>
                  {% if slide.description and slide.description != '' %}
                    <div class="slide-description">
                      {{ slide.description | editable }}
                    </div>
                  {% endif %}
                </div>
              </div>

            </{{tag}}>
          </div>
        {% endfor %}
      </div>

      <div class="promo_slider-pagination swiper-pagination swiper-pagination-white"></div>

      <div class="promo_slider-prev swiper-button-prev swiper-button-white"></div>
      <div class="promo_slider-next swiper-button-next swiper-button-white"></div>
    </div>
  </div>
{% endif %}
